/**
 * Module dependencies.
 */
const conf = require('../../../conf')('module1'); // require('ss-conf')('module1');

/**
 * Module implementation.
 */
// eslint-disable-next-line
console.log('filename: "module1.js" config = %o\n--', conf);
